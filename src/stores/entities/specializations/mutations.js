import { SPECIALIZATIONS_MUTATIONS } from '../../../common'

export default {
  [SPECIALIZATIONS_MUTATIONS.SET_SPECIALIZATIONS]: (state, payload) => {
    // eslint-disable-next-line
    state = payload;
  },
}
