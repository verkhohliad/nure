import { SPECIALITIES_MUTATIONS } from '../../../common'

export default {
  [SPECIALITIES_MUTATIONS.SET_SPECIALITIES]: (state, payload) => {
    // eslint-disable-next-line
    state = payload;
  },
}
